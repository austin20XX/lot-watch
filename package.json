{
  "devDependencies": {
    "@types/node": "20.12.7",
    "gts": "^5.3.1",
    "http-server": "^14.1.1",
    "typescript": "^5.4.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
